<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="80dp"
    android:id="@+id/constraintLayout"
    android:background="?attr/selectableItemBackground">

    <ImageView
        android:layout_width="56dp"
        android:layout_height="32dp"
        android:id="@+id/cardIssuerLogoImageView"
        android:contentDescription="@string/content_description_card_issuer_logo"
        android:layout_marginStart="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <TextView
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:id="@+id/cardIssuerNameTextView"
        android:text="Card Issuer Name"
        android:textColor="@color/colorPrimaryText"
        android:layout_marginStart="8dp"
        android:layout_marginEnd="8dp"
        app:layout_constraintTop_toTopOf="@id/cardIssuerLogoImageView"
        app:layout_constraintBottom_toBottomOf="@id/cardIssuerLogoImageView"
        app:layout_constraintStart_toEndOf="@id/cardIssuerLogoImageView"
        app:layout_constraintEnd_toEndOf="parent"/>

</android.support.constraint.ConstraintLayout>